{
  "servers": {
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "C:/project/vision"],
      "description": "Read/write files in the vision project"
    },
    "github": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": { "GITHUB_PERSONAL_ACCESS_TOKEN": "${env:GH_TOKEN}" },
      "description": "GitHub API — issues, PRs, repos, code search"
    },
    "memory": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "description": "Persistent knowledge graph — stores facts across sessions"
    },
    "puppeteer": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-puppeteer"],
      "description": "Browser automation — navigate, screenshot, extract web content"
    },
    "vision-tools": {
      "type": "http",
      "url": "http://localhost:8765/api/tool/execute",
      "description": "VISION operator tools — screen control, OCR, mouse/keyboard automation"
    }
  }
}
