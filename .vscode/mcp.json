{
  "servers": {
    "github": {
      "type": "http",
      "url": "https://api.githubcopilot.com/mcp/",
      "description": "GitHub Copilot MCP - issues, PRs, repos, code search"
    },
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "${workspaceFolder}"],
      "description": "Read and write files in the current Vision workspace"
    },
    "lmstudio-rag": {
      "type": "stdio",
      "command": "python",
      "args": ["launch_lmstudio_rag_mcp.py"],
      "description": "Read the user's LM Studio RAG plugin workspace from RAG_PLUGIN_WORKSPACE or the curated platform fallback"
    },
    "memory": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "description": "Persistent knowledge graph - stores facts across sessions"
    },
    "sequential-thinking": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"],
      "description": "Structured multi-step reasoning for complex tasks"
    },
    "puppeteer": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-puppeteer"],
      "description": "Browser automation - navigate, screenshot, extract web content"
    },
    "vision-local": {
      "type": "stdio",
      "command": "python",
      "args": ["vision_mcp_server.py"],
      "env": {
        "VISION_BASE_URL": "http://localhost:8765"
      },
      "description": "Repo-local FastMCP bridge for Vision health, memory, models, and tool execution"
    }
  }
}
